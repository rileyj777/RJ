# Claude Code — machine-local settings (permissions, MCP tokens, etc.)
.claude/settings.local.json

# OS / editor / OneDrive cruft
.DS_Store
Thumbs.db
desktop.ini

# Sensitive BD intel — third-party contacts + defense teaming strategy; keep local, never push
context/JE_fed-spend_playbook.md
context/JE_teaming-sprint.md
context/FUNDING_TRIAGE_*.md

# Python test/build cruft
__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/
